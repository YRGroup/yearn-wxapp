<view class="couse" style="background: url('{{img_base_url}}mainbg.jpg') repeat">
  <view class="title">
    <text>COURSE OFFERED</text>
    <text>课程设置</text>
  </view>
  <view class="item">
    <view class="list flex-row" wx:for="{{couseList}}" wx:key="{{index}}">
      <image class="left" src="{{item.url}}" mode="aspectFill"></image>
      <view class="right">
        <text class="couseTit">{{item.title}}</text>
        <text class="couseCon">{{item.caption}}</text>
      </view>
    </view>
  </view>
</view>